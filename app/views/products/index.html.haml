%h2
  All products
%ul
  - @products.each do |prod|
    %li
      = link_to prod.title, product_path(prod)
      - if prod.image
        = image_tag prod.image.picture.url(:thumb), class: "thumbnail"
      %p= prod.description
      %span.price.red= prod.price
      %p
        sku:
        = prod.sku
        category:
        %b{class: "#{prod.category.title.downcase}"}= prod.category.title