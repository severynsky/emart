.col-xs-10.col-xs-offset-1
  .pull-right
    = link_to 'back', products_path, class:("btn btn-default")
    = link_to 'edit', edit_product_path, class:("btn btn-info")
    = link_to 'delete', product_path, method: :delete, data: {confirm: "Are You sure?"}, class:("btn btn-danger")
  %hr
  .clear
  %h3
    = @product.title
    page
  %p
    -if @product.image
      = image_tag(@product.image.picture.url(:medium))
  %p
    = raw (@product.description)
  %b
    Price:
    %span.price.red
      = @product.price
  - if @product.category_id
    %p
      category:
      %b
        = @product.category.title
  - else
    %p
      currently
      %b
        no category
  = button_to "add to cart", product_records_path(product_id: @product), class: "btn btn-warning"
-#- binding.pry
.clear
%hr
